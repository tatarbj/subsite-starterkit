<?xml version="1.0" encoding="UTF-8" ?>

<project name="My subsite">

    <!-- Push the latest master to the temporary deploy repo. -->
    <target name="deploy">
        <echo msg="Push the latest master to the temporary deploy repo." />
        <bcdeploy
            subsiteRepository="${project.basedir}"
        />
    </target>

</project>
